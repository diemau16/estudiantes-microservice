name: CD with Docker

on:
  push:
    branches: [ main ]

jobs:
  build-and-push:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3
      - uses: actions/setup-java@v3
        with:
          distribution: corretto
          java-version: 17

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v1

      - name: Login to DockerHub
        uses: docker/login-action@v1
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Grant execute permission for gradlew
        run: chmod +x gradlew

      - name: Build with Gradle
        run: ./gradlew build -x test

      - name: Build and push
        uses: docker/build-push-action@v2
        with:
          context: .
          push: true
<<<<<<< Updated upstream
          tags: ed2723/estudiantes:latest
=======
          tags: ed2723/estudiantes:latest

      - name: Checkout Helm repo
        uses: actions/checkout@v3
        with:
            repository: helm-estudiantes
            ref: gh-pages
            token: ${{ secrets.PERSONAL_ACCESS_TOKEN }}
            path: helm

      - name: Update Helm chart
        run: |
            tar -zxvf ./estudiantes-0.1.0.tgz -C ./helm
            echo "image.tag=latest" >> ./estudiantes/values.yaml
            helm package ./estudiantes
            git config --global user.name "GitHub Actions"
            git config --global user.email "actions@github.com"
            git -C ./helm add .
            git -C ./helm commit -m "Update Helm chart"
            git -C ./helm push
>>>>>>> Stashed changes
